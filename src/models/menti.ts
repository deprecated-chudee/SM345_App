export class Menti {
    mento_id: number;
    menti_id: number;
    user_name: string;

    constructor(mento_id, menti_id, user_name) {
        this.mento_id = mento_id;
        this.menti_id = menti_id;
        this.user_name = user_name;
    }

}